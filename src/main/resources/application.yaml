ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080
db:
    mongo:
        user: $DB_USER
        password: $DB_PASSWORD
        url: $DB_URL
        database:
            name: $DB_NAME